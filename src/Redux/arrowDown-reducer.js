const initialState = {
  hash: {
    main: 'image_transfer',
    hotel: '',
    restaurant: '',
    services: '',
    contacts: ''
  }
}

const arrowDownReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default arrowDownReducer
